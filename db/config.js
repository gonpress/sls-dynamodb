module.exports.db = {
  region: 'localhost',
  endpoint: 'http://localhost:8000',
  accessKeyId: 'nh5k3o', // needed if you don't have aws credentials at all in env
  secretAccessKey: 'd8c1e', // needed if you don't have aws credentials at all in env
}
